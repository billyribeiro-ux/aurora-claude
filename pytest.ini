[pytest]
# Module 10 also ships a standalone unittest suite runnable with
#   python -m unittest discover -s 10_deployment/tests
# (kept separate because pytest can't import its digit-prefixed package).
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
